% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_plotting.R
\name{plot_volcano}
\alias{plot_volcano}
\title{Volcano plot of differential expression results}
\usage{
plot_volcano(
  results,
  fc_threshold = 1,
  p_threshold = 0.05,
  label_col = "protein"
)
}
\arguments{
\item{results}{A tibble (one row per protein) with at minimum the columns
\code{logFC}, \code{adj.P.Val}, and the column named by \code{label_col}.
Typically one element of the list returned by \code{run_limma_protein()}.}

\item{fc_threshold}{Numeric. Absolute log2 fold-change threshold. Defaults
to \code{1}.}

\item{p_threshold}{Numeric. Adjusted p-value threshold. Defaults to
\code{0.05}.}

\item{label_col}{Character. Column in \code{results} to use as hover-text
label. Defaults to \code{"protein"}.}
}
\value{
A \code{plotly} object.
}
\description{
Produces an interactive \code{plotly} scatter plot with
  \eqn{-\log_{10}(\text{adjusted p-value})} on the y-axis and
  \eqn{\log_2} fold change on the x-axis. Points are coloured by
  significance category: \emph{Up}, \emph{Down}, or \emph{NS}. Dashed
  threshold lines are drawn as reference guides.
}
\details{
Plot a volcano plot for a single Limma result table
}
\examples{
\dontrun{
pv <- plot_volcano(
  results      = limma_results[["A - B"]],
  fc_threshold = 1,
  p_threshold  = 0.05,
  label_col    = "Gene_name"
)
pv
}

}
