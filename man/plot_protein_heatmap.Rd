% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_plotting.R
\name{plot_protein_heatmap}
\alias{plot_protein_heatmap}
\title{Interactive heatmap of protein intensities across samples}
\usage{
plot_protein_heatmap(
  data,
  metadata,
  proteins,
  scale_rows = TRUE,
  exclude_groups = NULL
)
}
\arguments{
\item{data}{A long-form tibble with columns \code{id}, \code{Sample_name},
and \code{Intensity}.}

\item{metadata}{A tibble with columns \code{Sample_name} and \code{Group}.}

\item{proteins}{A character vector of \code{id} values to include.}

\item{scale_rows}{Logical. If \code{TRUE} (default), rows are z-score scaled.}

\item{exclude_groups}{A character vector of group labels to exclude from the
heatmap. Defaults to \code{NULL}.}
}
\value{
A \code{plotly}/\code{heatmaply} object.
}
\description{
Produces an interactive \code{heatmaply} heatmap showing
  normalised intensities for a selected set of proteins. Rows are optionally
  z-score scaled. Groups listed in \code{exclude_groups} are removed before
  plotting.
}
\details{
Plot an interactive protein-level heatmap
}
\examples{
\dontrun{
sig_proteins <- limma_results[["A - B"]] |>
  dplyr::filter(adj.P.Val < 0.05, abs(logFC) > 1) |>
  dplyr::pull(protein)

ph <- plot_protein_heatmap(
  data       = normalised_long,
  metadata   = sample_table,
  proteins   = sig_proteins
)
ph
}

}
